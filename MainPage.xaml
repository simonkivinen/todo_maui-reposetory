<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="todoapp.MainPage">

    <Grid BackgroundColor="#D6A82A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Text="Category list"
               FontSize="24"
               FontAttributes="Bold"
               VerticalOptions="Start"
               HorizontalOptions="Start"
               Margin="10"
               Grid.Row="0" 
               Grid.Column="0" />

        <StackLayout Grid.Row="0" Grid.Column="0" Margin="10">
            <Button Text="Add list +" BackgroundColor="#FFA500" Clicked="OnAddListClicked" />
            <Entry x:Name="TaskEntry" Placeholder="Enter new task/title" Margin="0,10,0,10" />
            <Entry x:Name="DescriptionEntry" Placeholder="Enter task description" />
        </StackLayout>

        <ScrollView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10">
            <StackLayout x:Name="TaskList" BackgroundColor="#B0BEC5" Padding="10" />
        </ScrollView>
    </Grid>
</ContentPage>